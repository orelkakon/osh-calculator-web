import React from 'react';

const GenderButton = () => {
    return (
        <div>
            <button>זכר</button>
            <button>נקבה</button>
        </div>
    );
};

export default GenderButton;